#include <stdio.h>
#define MAX 100
int main()
{
	int x[MAX],n,p,n,i,pmax,contor=0,min,s=0,minim=999;maxim=-999;
	float medie;
	scanf("%d",&n);
		for=0;i<n;i++) {
		scanf("%d",&x[i]);
		}
			for(i=0;i<n;i++) 
			{
				if (v[i]<minim)
				min=v[i];
				printf("min = %d",min);
					for(i=0;i<n;i++)
					{
						if (v[i]>maxim){
						pmax=i;
						printf("poz_max = %d",pmax);
						}
							for(i=0;i<n;i++)
							{
							s+=v[i];
							medie=s/n;
							printf("ma = %.2f",medie);
								for(i=0;i<n;i++)
								{
								if (v[i]>medie){
								contor+=1;
								printf("gt_ma = %d",p);
								}	
								}
							}
					}
			}
}
return 0;				
				
