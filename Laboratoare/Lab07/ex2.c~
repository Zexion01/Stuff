#include <stdio.h>
int string_ln(char*p)
{
   int count = 0;
   while (*p != '\0') 
   {
      count++;
      p++;
   }
   return count;
}

int main(void)
{
	char x[50]="abd";
	int lungime;
	lungime = string_ln(x);
	printf("%d\n",lungime);
	return 0;
}
